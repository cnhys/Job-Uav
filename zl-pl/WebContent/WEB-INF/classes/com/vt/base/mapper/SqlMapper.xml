<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vt.base.client.SqlMapper">
    <select id="getBySql" parameterType="java.lang.String" resultType="java.util.Map">
        ${value}
    </select>
    <select id="getBySqlCount" parameterType="java.lang.String" resultType="java.lang.Integer">
        select count(*) from
        (
        ${value}
        ) aBcDeF
    </select>
    <insert id="insertBySql" parameterType="java.lang.String">
        ${value}
    </insert>
    <update id="updateBySql" parameterType="java.lang.String">
        ${value}
    </update>
    <delete id="deleteBySql" parameterType="java.lang.String">
        ${value}
    </delete>
</mapper>